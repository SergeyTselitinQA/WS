# Объяви переменные: a = 5, b = "10". Выведи их типы (type()).
a = 5
b = "10"

print(type(a))
print (type(b))

# Преобразуй b в число и выведи сумму a + int(b).

b = int(b)
print(a + b)

# Создай список lst = [1,2,3]. Добавь элемент 4 и выведи lst.

lst = [1,2,3]
lst.append(4)
print(lst)

# Создай словарь user = {"id": 1, "name": "Alex"}. Выведи значение по ключу "name".

user = {"id": 1, "name": "Alex"}
print(user["name"])

# Создай список lst = [1, "2", 3.0]. Приведи все элементы к типу int и выведи новый список.
lst = [1, "2", 3.0]
new_list = list(map(int, lst))
print(new_list)

# Дана строка s = "3.14". Преобразуй её в число с плавающей точкой и умножь на 2. Выведи результат и его тип.
s = "3.14"
print(type(float(s)))
# Создай пустое множество, добавь элементы 1, 2, 2, "2". Выведи множество и объясни, сколько элементов в нём и почему.
s = set()
s.add(1)
s.add(2)
s.add(2)
s.add("2")
print(s)
print(len(s))

# Объяви кортеж t = (1, 2, 3). Попробуй заменить t[1] = 10, поймай и выведи исключение. Затем создай новый кортеж с заменой второго элемента на 10.

# Дана строка data = "id:42,name:Bob,age:30". Разбери её и создай словарь {"id": 42, "name": "Bob", "age": 30} (обрати внимание на приведение типов).

# Есть словарь user = {"id": "5", "scores": ["10", "20", "30"]}. Преобразуй id в int и все элементы в scores в int, затем выведи сумму баллов.

# Напиши функцию safe_int(x) которая пытается вернуть int(x). Если преобразование невозможно, возвращает None и не вызывает ошибку. Проверь функцию
# на входах: "7", "7.5", "abc", None.

# Даны два множества a = {1,2,3}, b = {3,4,5}. Выведи их объединение, пересечение и разность a - b. Убедись, что результат — множества.

# Дан список строк nums = ["1", "2", "three", "4"]. Преобразуй только те элементы, которые можно в int, в целые числа; остальные пропусти.
# Результат — новый список целых чисел. (Используй обработку исключений.)